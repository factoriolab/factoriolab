import { Option } from '~/option/option';

export interface Options {
  beacons: Option[];
  belts: Option[];
  categories: Option[];
  fuels: Option[];
  inserters: Option[];
  locations: Option[];
  machines: Option[];
  modules: Option[];
  proliferatorModules: Option[];
  qualities: Option[];
  wagons: Option[];
}
