import { Option } from '~/option/option';

export interface Options {
  categories: Option[];
  beacons: Option[];
  belts: Option[];
  pipes: Option[];
  cargoWagons: Option[];
  fluidWagons: Option[];
  fuels: Option[];
  modules: Option[];
  proliferatorModules: Option[];
  machines: Option[];
  locations: Option[];
}
