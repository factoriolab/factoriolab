# Docker Registry Configuration
# Leave DOCKER_REGISTRY empty for DockerHub (default)
# For other registries, use the full URL (e.g., ghcr.io, gcr.io, etc.)
DOCKER_REGISTRY=

# Docker image name (without registry prefix)
DOCKER_IMAGE_NAME=factoriolab

# Optional: Docker username for authentication
DOCKER_USERNAME=

# Optional: Docker password/token for authentication (use access tokens for security)
DOCKER_PASSWORD=

# Target platforms for multi-arch builds (comma-separated)
# Default: linux/amd64,linux/arm64
DOCKER_PLATFORMS=
